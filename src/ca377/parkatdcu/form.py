# from django import forms
# from .models import Campus


# camps = Campus.objects.all()
# campi = []
# for camp in camps:
#     campi.append((camp.name, camp.name))


# class CarparkForm(forms.Form):
#     campus = forms.ChoiceField(label="campus", choices=campi, widget=forms.Select(attrs={'class': "form-control"}))
